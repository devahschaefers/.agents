ALWAYS create a worktree for your work (put it in `~/.worktrees/{repo name}/`). When the work is done, ask whether to merge to main or put up a PR.

Commit frequently, especially when doing large changes.
